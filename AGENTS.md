# AGENTS.md

This file provides guidance to WARP (warp.dev) when working with code in this repository.

## Repository status
- The repository currently only contains `README.md` with the title “Ena”. No source files or build/test tooling were found at the time of writing.

## Common commands
- No build, lint, or test commands were found in the repository. Add them here when tooling is introduced.
- Git workflow:
  - `git add .`
  - `git commit -m "the changes summary"`
  - `git push`
  - the the "changes summary" is what you have to summarize the changes and rename it every commits.
  - alias: commit
## High-level architecture
- Not available yet. No application or library code is present. Update this section when modules or services are added.
